Set the Theme
-------------

In the docs directory, update ``conf.py`` to set the Read the Docs theme. The default is ``alabaster`` (ugly):

::

	html_theme = 'sphinx_rtd_theme'

Create a Pages Directory and a Page
-----------------------------------
::

$ mkdir pages
$ cd pages
$ touch page1.rst